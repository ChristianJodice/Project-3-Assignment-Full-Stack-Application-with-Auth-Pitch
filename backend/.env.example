# Copy to .env and change values. Never commit .env.

SECRET_KEY=generate-a-long-random-string
JWT_SECRET_KEY=another-long-random-string
# Default (if unset): backend/instance/gearledger.db — see gearledger/config.py
# DATABASE_URL=sqlite:///C:/absolute/path/to/gearledger.db

# Comma-separated origins for browser requests (production / deployed frontend)
# CORS_ORIGINS=https://your-frontend.onrender.com
